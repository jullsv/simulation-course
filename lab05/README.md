### Моделирование случайных событий

**Часть 1:**  
Приложение «Скажи “да” или “нет”».

**Часть 2:**  
Приложение «Шар предсказаний» (Magic 8-Ball).

### Выполнение работы
Для двух приложений мы используем мультипликативный конгруэнтный генератор псевдослучайных чисел из предыдущей лабораторной работы:

```python
class BaseGenerator:
    def __init__(self, b, m, seed):
        self.b = b   
        self.m = m     
        self.x = seed    
    
    def rand(self):
        self.x = (self.b * self.x) % self.m
        return self.x / self.m
```

### Теория 1
Вероятность события можно представить геометрически как отрезок на интервале [0, 1]:

$$P\{\text{random} < p\} = p$$

где random — случайная величина, равномерно распределённая на [0, 1], а p — вероятность наступления события.

В коде используется следующий алгоритм:

1. A := α (генерируем случайную величину)
2. k := 1
3. A := A - pₖ
4. Если A ≤ 0 → произошло событие Aₖ
5. Иначе k := k + 1, повторить шаг 3


### Теория 2
Полная группа событий

События $A_1, A_2, ..., A_m$ образуют полную группу, если:

$$\sum_{i=1}^{m} p_i = 1$$

где $p_i$ = P{Aᵢ} — вероятность наступления события $A_i$.

## Приложение «Скажи 'да' или 'нет'»

<img width="690" height="659" alt="image" src="https://github.com/user-attachments/assets/ee5d3583-2f0e-4b02-99d9-b924308a77cf" />

*Рисунок 1 - окно интерфейса приложения "Дай ответ"*

Алгоритм кода:

1. Пользователь вводит вопрос
2. Пользователь задаёт вероятность события «ДА» (от 0 до 1)
3. Генерируется случайная величина α с помощью базового датчика
4. Если α < p - событие «ДА» произошло
5. Иначе - событие «НЕТ»

```python
alpha = self.generator.rand()
if alpha < prob:
    result = "ДА!"
else:
    result = "НЕТ!"
```
## Шар предсказаний

<img width="863" height="1261" alt="image" src="https://github.com/user-attachments/assets/5c625270-8b8d-4f47-be7e-a846aa4c2255" />

*Рисунок 2 - интерфейс приложения "Шар предсказаний"*

Алгоритм кода:
1. Ввод пользователем вопроса
2. Задание вероятностей для первых 9 ответов
3. Последняя вероятность вычисляется автоматически
4. Генерация ответа с помощью базового датчика

```python
def get_answer(self):
    # Получаем вероятности от пользователя
    probs = []
    for entry in self.prob_entries:
        p = float(entry.get())
        probs.append(p)
    
    # Вычисляем последнюю вероятность
    last_prob = 1.0 - sum_probs
    probs.append(last_prob)
    
    # Генерируем случайную величину
    alpha = self.generator.rand()
    
    # Находим номер интервала
    A = alpha
    k = 0
    while k < len(probs):
        A = A - probs[k]
        if A <= 0:
            break
        k = k + 1
    
    #  Возвращаем ответ
    answer = self.answers[k]
```
В двух частях лабораторной был добавлен прогрев генератора (100 итераций перед использованием) для разнообразности ответов.

## Вывод

В ходе выполнения лабораторной работы были изучены и реализованы на практике основные принципы моделирования случайных событий с использованием геометрического смысла вероятности. Созданы два работающих приложения с GUI на tkinter. В работе не использовались встроенные датчики python, а взят уже реализованный в прошлой лабораторной, собственный базовый датчик случайных чисел (мультипликативный конгруэнтный генератор псевдослучайных чисел).
